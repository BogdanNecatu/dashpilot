import LoginForm from "@/features/login/ui/LoginForm";

export default function LoginPage() {
  return (
    <section className="h-[70dvh] flex items-center justify-center bg-background px-4">
      <LoginForm />
    </section>
  );
}
